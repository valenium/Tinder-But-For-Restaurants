<%- include('../partials/header.ejs') %>
<% let likeCount = {} %>
<% user.likes.forEach(like => likeCount[like.like] = (likeCount[like.like] || 0) + 1) %>
<% if(likeCount['Dislike'] > 0) { %>
    <% user.likes.forEach(function(like) { %>
        <% if(like.like === 'Dislike') {%>
        <div class="restaurant">
            <a href='/restaurants/<%=like.restaurant._id%>/reviews'><%= like.restaurant.name %></a>
        </div>
        <% } %>
    <% }) %>
    <div class="main-buttons">
        <button style="width: 13.5rem;" onclick="history.back(-1)">Back</button> 
        <a href="/users/<%=user._id%>/dislikes/edit"><button style="width: 13.5rem;">Edit Disliked Restaurants</button></a>

<% } else { %>
    <p>You haven't disliked any restaurants yet!</p>
<% } %>
<%- include('../partials/footer.ejs') %>